/// @description Insert description here
// You can write your code in this editor
if (attacker != noone && defender != noone) {
	depth = -100;
	draw_set_color(c_red);
	draw_line(attacker.x, attacker.y, defender.x, defender.y);
}